// This icon file is generated automatically.
import * as React from 'react'
import LoadingFilledSvg from '@lotus-design/icons-svg/lib/asn/LoadingFilled';
import LotusIcon, { LotusIconProps } from '../components/LotusIcon';

const LoadingFilled = (props, ref) =>
  <LotusIcon {...props} ref={ref} icon={LoadingFilledSvg} />;

LoadingFilled.displayName = 'LoadingFilled';

export default React.forwardRef<HTMLSpanElement, LotusIconProps>(LoadingFilled);
